import 'package:flutter/material.dart';
class bluetooth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: new Text("Bluetooth")),
      ),
    );
  }
}