import 'package:flutter/material.dart';
class wifi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: new Text("Wifi")),
      ),
    );
  }
}